import { MarcaFilterPipe } from './marca-filter.pipe';

describe('MarcaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MarcaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
