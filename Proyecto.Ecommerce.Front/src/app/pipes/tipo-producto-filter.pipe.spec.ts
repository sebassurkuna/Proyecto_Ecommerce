import { TipoProductoFilterPipe } from './tipo-producto-filter.pipe';

describe('TipoProductoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoProductoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
